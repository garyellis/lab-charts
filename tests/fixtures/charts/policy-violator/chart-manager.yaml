version: 1
enabled: true

manifestValidation:
  enabled: true
  releaseName: policy-violator
  environments:
    dev:
      namespace: lab-dev
      values:
        - values.yaml
  triggers:
    "values.yaml": [dev]
  unmatchedChanges: warn
