version: 1
release_name: flink-operator
namespace_template: "lab-${env}"
environments:
  ci:
    namespace: flink-operator
    values:
      - values.yaml
      - values-ci.yaml
triggers:
  "Chart.yaml":     [ci]
  "values.yaml":    [ci]
  "values-ci.yaml": [ci]
  "templates/**":   [ci]
