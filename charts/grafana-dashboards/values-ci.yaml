---
# Validate every committed group. Production consumers must select only the
# groups they own; obs-w selects ai1-openstack alone.
enabledGroups:
  - ai1-openstack
  - networking
  - platform
  - slo
