# Select one or more reviewed directories under dashboards/. This value is
# intentionally required: a consumer must opt into a bounded dashboard set.
# CI supplies its explicit selection in values-ci.yaml.
# enabledGroups:
#   - ai1-openstack

# Extra non-reserved labels applied to every rendered ConfigMap.
additionalLabels: {}
  # team: platform
  # component: observability
