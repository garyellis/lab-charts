version: 1
release_name: harbor
namespace_template: "lab-${env}"
environments:
  ci:
    namespace: harbor
    values:
      - values.yaml
      - values-ci.yaml
triggers:
  "Chart.yaml":     [ci]
  "values.yaml":    [ci]
  "values-ci.yaml": [ci]
  "templates/**":   [ci]
