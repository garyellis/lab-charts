version: 1

profiles:
  minimal:
    description: Install node-exporter with ServiceMonitor CRDs available.
    requires:
      - chart: prometheus-operator
        profile: minimal
    values:
      - values.yaml
      - values-ci.yaml
    helmTest: true
    timeout: 10m

reverseTests: []
