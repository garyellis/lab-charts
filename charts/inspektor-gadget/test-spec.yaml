version: 1

profiles:
  minimal:
    description: Install Inspektor Gadget DaemonSet and verify it rolls out on every node.
    namespace: gadget
    requires: []
    values:
      - values.yaml
      - values-ci.yaml
    helmTest: true
    timeout: 10m

reverseTests: []
