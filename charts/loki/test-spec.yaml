version: 1

profiles:
  minimal:
    description: Install Loki with CI-local values.
    requires: []
    values:
      - values.yaml
      - values-ci.yaml
    helmTest: true
    timeout: 15m

reverseTests:
  - chart: grafana
    profile: with-deps
  - chart: alloy
    profile: telemetry
