"""The ``lifecycle.cmg.io`` API group.

Import the explicit version rather than this package::

    from chart_manager.api.lifecycle.v1alpha1 import ChartLifecycle

This module deliberately re-exports nothing, so adding a future version
cannot silently change what an existing consumer parses.
"""
